# 航线非卡工时匹配项目指南

## 项目简介

本项目用于处理飞机维修工单的工时匹配，包含 Python 数据处理脚本和 WPS AirScript 自动化排版脚本。

## 核心文件

- `worktime_processor.py`: 主程序（旧版）。读取工单和标准库，进行匹配计算，生成“匹配结果”和“未匹配结果”。
- `worktime_processor.asc`: **工时匹配 AirScript 版**。直接在 WPS JS 编辑器中运行，逻辑与 Python 版一致，支持大数据量分块读写。
- `wps_section_matcher.py`: **新版工段匹配脚本**。用于 WPS 环境，根据“控制号”匹配标准工时，并根据“完成情况”提取工段（一工段/二工段/未完成）。
- `wps_airscript_merge_sort.js`: 后处理脚本。在 WPS 中运行，用于对生成的结果进行排序、合并单元格、居中和随机着色。

## 关键业务逻辑

### 1. 工时匹配逻辑 (`worktime_processor.py` / `worktime_processor.asc`) [NEW]

- **适用场景**: 
  - Python 版：适合在 WPS PY 脚本编辑器中运行。
  - AirScript 版：适合在 WPS JS 脚本编辑器中运行。
- **输入表**:
  - `导出的待匹配工时内容`: 待匹配工单。
  - `工时参考标准文件`: 标准工时库。
  - `工段分布`: 员工号 -> 工段映射。
- **匹配规则**:
  - **清洗**: 机型标准化（A320/A321 -> A320S），依据文件去 REV、去空格、提取前缀+数字。
  - **索引**: 建立 `(机型, 编码)` 的完全匹配索引，以及 `(机型, 前缀)` 的近似匹配索引。
  - **优先级**: 完全匹配 > 近似匹配（最长公共前缀）。
- **工段提取**: 自动扫描 `工段分布` 表，根据“员工号”定位并提取上方工段名。
- **输出**:
  - `匹配结果`: 包含工段汇总行。
  - `未匹配结果`: 包含原因统计。

### 2. Python 处理逻辑 (`worktime_processor.py`) [OLD]

- **输入表**:
  - `导出的待匹配工时内容` (源数据)
  - `工时参考标准文件` (标准库)
  - `工段分布` (员工号 -> 工段映射)
- **数据增强逻辑**:
  - **机型补全**: 若机型缺失，尝试通过“依据文件”推断。
  - **编码标准化**: 去空格、去 REV、保留关键横杠。
- **匹配规则**:
  - **完全相等**: 机型 + 标准化编码。
  - **版本映射**: 前缀近似匹配。
- **输出结构**:
  - `匹配结果` / `未匹配结果` / CSV 清单 / 统计报告。

### 3. WPS AirScript 逻辑 (`wps_airscript_merge_sort.js`)

- **功能**:
  - 对“匹配结果”和“未匹配结果”表进行美化。
  - **A列合并**: 相同内容的连续行合并并居中。
  - **汇总行处理**: 自动识别底部的“汇总”行，合并居中。
  - **随机着色**: 对合并后的 A 列块应用随机浅色背景。

## 开发规范

- **Python**:
  - 使用 4 空格缩进。
  - 避免 `DataFrame.append`，使用 `pd.concat`。
  - 兼容 WPS 环境：使用 `xl()`, `write_xl()`, `delete_xl()`，避免 `openpyxl` 或本地文件路径。
- **AirScript**:
  - 兼容 WPS JS API。

## 常用操作

1. **新版工段匹配**:
   - 在 WPS 中打开 Excel 文件。
   - 确保包含 `工时参考标准文件` 和待匹配数据的 Sheet。
   - 打开“效率” -> “PY脚本编辑器”。
   - 粘贴 `wps_section_matcher.py` 代码并运行。
   - 查看生成的 `工段工时统计` 表。

2. **旧版流程**:
   - 运行本地 `worktime_processor.py` 生成数据。
   - 在 WPS 中运行 AirScript 美化。

## 方案先行

   写代码前必须先制定方案并获得批准，避免需求不明确导致无效代码。

## 任务拆解

   超过 3 个文件的改动需分段处理，防止 AI 因任务复杂度过高而 “脑载荷” 过大。

## 防御性编程

   写完代码后需附带可能出现的 Bug 清单和测试用例，提前预防问题。

## TDD 模式（测试驱动开发）

   先编写能复现 Bug 的测试脚本，再根据测试结果修复代码，确保问题彻底解决。

## 自我进化

   每次 AI 纠正代码后，自动更新 AGENTS.md 中的规则，避免同类错误再次发生。
